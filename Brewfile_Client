# Taps
tap "homebrew/bundle"

# Apps
cask "apache-directory-studio" if OS.mac?
cask "camunda-modeler" if OS.mac?
cask "citrix-workspace" if OS.mac?
cask "confluent-cli" if OS.mac?
cask "jprofiler" if OS.mac?
cask "keystore-explorer" if OS.mac?
cask "lens" if OS.mac?
cask "miro" if OS.mac?
