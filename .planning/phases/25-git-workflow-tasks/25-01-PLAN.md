---
phase: 25-git-workflow-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - private_dot_config/mise/tasks/git/executable_commit
  - private_dot_config/mise/tasks/git/executable_branch
autonomous: true

must_haves:
  truths:
    - "User can run git:commit (or alias c) to create a conventional commit with Jira ticket prefix"
    - "User can run git:branch (or alias b) to create a feature branch following naming convention"
    - "Jira ticket format validated as PROJECT-123 pattern"
    - "Commit format follows <ticket>: <type>[scope]: <description>"
    - "Branch format follows feature/<ticket>-<description>"
    - "Both tasks offer AI-assisted mode (via claude CLI) with manual fzf fallback"
  artifacts:
    - path: "private_dot_config/mise/tasks/git/executable_commit"
      provides: "Hybrid AI/manual conventional commit workflow"
      contains: "MISE description"
    - path: "private_dot_config/mise/tasks/git/executable_branch"
      provides: "Hybrid AI/manual feature branch creation"
      contains: "MISE description"
  key_links:
    - from: "executable_commit"
      to: "git commit"
      via: "claude CLI for AI mode, fzf + bash read for manual mode"
      pattern: "git commit -m"
    - from: "executable_branch"
      to: "git checkout -b"
      via: "claude CLI for AI mode, bash read for manual mode"
      pattern: "git checkout -b"
---

<objective>
Implement git:commit and git:branch mise tasks with hybrid AI/manual mode, Jira ticket validation, and convention enforcement.

Purpose: Provide guided workflows for creating conventional commits and feature branches. AI mode (via `claude` CLI) generates messages/names from diffs and context; manual mode uses fzf + bash read. Falls back to manual when `claude` CLI is unavailable.
Output: Two executable task files deployed via chezmoi to ~/.config/mise/tasks/git/
</objective>

<execution_context>
@/Users/stephanlv_fanaka/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephanlv_fanaka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-git-workflow-tasks/25-RESEARCH.md
@private_dot_config/mise/tasks/dotfiles/executable_apply
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement git:commit hybrid AI/manual task</name>
  <files>private_dot_config/mise/tasks/git/executable_commit</files>
  <action>
Replace the existing stub in `executable_commit` with the hybrid AI/manual conventional commit workflow.

The script must:
1. Validate staged changes exist (`git diff --cached --quiet` — exit 1 if no staged changes)
2. Prompt for Jira ticket with regex validation `^[A-Z]+-[0-9]+$`
3. Mode selection: if `claude` CLI available, present fzf choice of `ai` / `manual`; default to `manual` if unavailable
4. **AI mode:** pipe `git diff --cached` to `claude --print -p` with prompt to generate conventional commit message in format `${TICKET}: <type>[scope]: <description>`. Validate output starts with ticket prefix; fall back to manual if not.
5. **Manual mode:** fzf type selection (feat, fix, chore, docs, style, refactor, test, perf, ci, build, revert), optional scope, description prompt, breaking change flag
6. Both paths converge: show preview and confirm (Y/n) before executing `git commit -m`

Use #MISE directives: description="Create conventional commit with Jira prefix", alias="c"
Use `set -euo pipefail`. All errors to stderr.

Reference the research code example in 25-RESEARCH.md "GIT-01: Interactive Commit Task (Hybrid AI/Manual)" section.
  </action>
  <verify>
Run `chezmoi apply` then verify:
- `mise tasks | grep git:commit` shows task with description
- `mise run c --help` resolves to git:commit
- File at `~/.config/mise/tasks/git/commit` is executable
- Script contains `command -v claude` check, fzf mode selection, AI path with `claude --print`, manual fzf fallback
- Script contains Jira validation regex, git commit command
  </verify>
  <done>git:commit task deployed with alias "c", hybrid AI/manual mode, validates Jira ticket format, AI generates message from diff, manual uses fzf, confirms before committing</done>
</task>

<task type="auto">
  <name>Task 2: Implement git:branch hybrid AI/manual task</name>
  <files>private_dot_config/mise/tasks/git/executable_branch</files>
  <action>
Create `executable_branch` in the chezmoi source git task directory.

The script must:
1. Check current branch — warn if not on main/master, require confirmation to continue
2. Prompt for Jira ticket with regex validation `^[A-Z]+-[0-9]+$`
3. Mode selection: if `claude` CLI available, present fzf choice of `ai` / `manual`; default to `manual` if unavailable
4. **AI mode:** prompt user for free-text work description, pipe to `claude --print -p` to convert to kebab-case branch suffix (2-4 words). Validate output is kebab-case; fall back to manual if not.
5. **Manual mode:** prompt for kebab-case description directly
6. Both paths converge: construct `feature/${TICKET}-${DESC}`, check doesn't exist, show preview, confirm (Y/n) before `git checkout -b`

Use #MISE directives: description="Create feature branch with naming convention", alias="b"
Use `set -euo pipefail`. All errors to stderr.

Reference the research code example in 25-RESEARCH.md "GIT-02: Branch Creation Task (Hybrid AI/Manual)" section.
  </action>
  <verify>
Run `chezmoi apply` then verify:
- `mise tasks | grep git:branch` shows task with description
- `mise run b --help` resolves to git:branch
- File at `~/.config/mise/tasks/git/branch` is executable
- Script contains `command -v claude` check, AI path with `claude --print`, manual fallback
- Script contains Jira validation, branch existence check, git checkout -b
  </verify>
  <done>git:branch task deployed with alias "b", hybrid AI/manual mode, validates Jira ticket, AI converts free-text to kebab-case branch name, manual prompts directly, warns when not on main/master</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `chezmoi apply` deploys both files with executable permissions
2. `mise tasks | grep git:` shows both commit and branch tasks
3. Aliases c and b resolve correctly
4. Both scripts contain Jira ticket validation regex
5. Shell startup time under 300ms (run `zsh-startup-bench`)
</verification>

<success_criteria>
- git:commit task creates conventional commits with format <ticket>: <type>[scope]: <description>
- git:branch task creates branches with format feature/<ticket>-<description>
- Both tasks validate Jira ticket format (PROJECT-123)
- Both tasks offer AI mode (claude CLI) with graceful fallback to manual (fzf)
- Aliases c and b work for quick access
</success_criteria>

<output>
After completion, create `.planning/phases/25-git-workflow-tasks/25-01-SUMMARY.md`
</output>
