---
phase: 18-clean-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/profile
  - scripts/audit-report-20260208-162401.md
  - scripts/audit-report-20260208-162439.md
  - scripts/audit-report-20260208-162740.md
  - scripts/audit-report-20260208-162806.md
  - scripts/audit-report-20260212-210756.md
  - scripts/audit-report-initial.md
  - scripts/verify-checks/11-claude-code.sh
  - scripts/verify-checks/12-dotbot-retirement.sh
  - scripts/test-linux.sh
  - .docker/Dockerfile.dotfiles-test
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Repository contains no orphaned .config/profile or empty .config/ directory"
    - "Repository contains no audit-report-*.md files in scripts/"
    - "Repository contains no obsolete verify-checks (11-claude-code.sh, 12-dotbot-retirement.sh)"
    - "Repository contains no non-functional test-linux.sh or its Dockerfile"
    - ".gitignore prevents future audit-report-*.md accumulation"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules"
      contains: "audit-report"
  key_links: []
---

<objective>
Delete orphaned files and obsolete scripts identified by the v1.2 milestone audit, and ensure .gitignore prevents re-accumulation.

Purpose: Final tech debt cleanup so the repository contains only active, functional code.
Output: Clean repository with 11 dead files removed and .gitignore updated.
</objective>

<execution_context>
@/Users/stephanlv_fanaka/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephanlv_fanaka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned and obsolete files</name>
  <files>
    .config/profile
    scripts/audit-report-20260208-162401.md
    scripts/audit-report-20260208-162439.md
    scripts/audit-report-20260208-162740.md
    scripts/audit-report-20260208-162806.md
    scripts/audit-report-20260212-210756.md
    scripts/audit-report-initial.md
    scripts/verify-checks/11-claude-code.sh
    scripts/verify-checks/12-dotbot-retirement.sh
    scripts/test-linux.sh
    .docker/Dockerfile.dotfiles-test
  </files>
  <action>
    Delete the following files via `git rm`:

    1. **Orphaned .config/profile** — Phase 14 removed san-proxy sourcing but left the file. No active code references it. Contains stale Volta PATH and generic PATH additions already handled by chezmoi source. After deletion, remove the now-empty `.config/` directory.

    2. **6 audit report files** — Generated artifacts from Feb 8-12, cluttering scripts/:
       - `scripts/audit-report-20260208-162401.md`
       - `scripts/audit-report-20260208-162439.md`
       - `scripts/audit-report-20260208-162740.md`
       - `scripts/audit-report-20260208-162806.md`
       - `scripts/audit-report-20260212-210756.md`
       - `scripts/audit-report-initial.md`

    3. **2 obsolete verify-checks** — v1.1 migration verification scripts, no longer needed:
       - `scripts/verify-checks/11-claude-code.sh`
       - `scripts/verify-checks/12-dotbot-retirement.sh`

    4. **test-linux.sh and its Dockerfile** — The script references `./install` which was removed in Phase 12 (Dotbot retirement), making it non-functional. The Dockerfile it depends on is also orphaned:
       - `scripts/test-linux.sh`
       - `.docker/Dockerfile.dotfiles-test`
       After deletion, remove the now-empty `.docker/` directory.

    Use a single `git rm` command for all files, then verify no empty directories remain (.config/ and .docker/ should be gone).
  </action>
  <verify>
    - `ls .config/profile` returns "No such file"
    - `ls .config/` returns "No such file" (directory removed)
    - `ls scripts/audit-report-*.md` returns "No such file"
    - `ls scripts/verify-checks/11-claude-code.sh scripts/verify-checks/12-dotbot-retirement.sh` returns "No such file"
    - `ls scripts/test-linux.sh` returns "No such file"
    - `ls .docker/` returns "No such file" (directory removed)
  </verify>
  <done>All 11 orphaned/obsolete files deleted, empty .config/ and .docker/ directories removed</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore to prevent re-accumulation</name>
  <files>.gitignore</files>
  <action>
    The .gitignore already contains `scripts/audit-report-*.md` (line 27). Verify this entry exists and is correct — no further changes needed to .gitignore for audit reports.

    Additionally add ignore entries for the now-removed directories to prevent accidental re-creation:
    - `.config/` (legacy Dotbot-era directory, chezmoi source is in ~/.local/share/chezmoi)
    - `.docker/` (orphaned test infrastructure)

    Add these under the existing "Legacy directories" comment section, alongside the existing `zsh.d/` entry.

    Do NOT add `scripts/test-linux.sh` or verify-checks to .gitignore — they are one-off deletions, not recurring patterns.
  </action>
  <verify>
    - `grep 'audit-report' .gitignore` shows the existing pattern
    - `grep '\.config/' .gitignore` shows the new entry
    - `grep '\.docker/' .gitignore` shows the new entry
  </verify>
  <done>.gitignore prevents future audit-report, .config/, and .docker/ accumulation</done>
</task>

</tasks>

<verification>
Run from repository root:
1. `test ! -f .config/profile` — profile gone
2. `test ! -d .config` — directory gone
3. `test ! -d .docker` — directory gone
4. `ls scripts/audit-report-*.md 2>&1 | grep -q "No such file"` — no audit reports
5. `test ! -f scripts/verify-checks/11-claude-code.sh` — obsolete check gone
6. `test ! -f scripts/verify-checks/12-dotbot-retirement.sh` — obsolete check gone
7. `test ! -f scripts/test-linux.sh` — test script gone
8. `grep -q 'audit-report' .gitignore` — .gitignore blocks reports
9. `grep -q '\.config/' .gitignore` — .gitignore blocks .config/
</verification>

<success_criteria>
- Repository contains no .config/profile or .config/ directory
- Repository contains no audit-report-*.md files in scripts/
- Repository contains no 11-claude-code.sh or 12-dotbot-retirement.sh verify-checks
- scripts/test-linux.sh and .docker/Dockerfile.dotfiles-test removed (non-functional)
- .gitignore prevents future re-accumulation of audit reports and legacy directories
</success_criteria>

<output>
After completion, create `.planning/phases/18-clean-tech-debt/18-01-SUMMARY.md`
</output>
